
--
-- Component constructors
--

CMP = {
  transform = require("cmp.transform"),
  physics   = require("cmp.physics"),
  render    = require("cmp.render"),
  ai        = require("cmp.ai"),
  collision = require("cmp.collision"),
}

return CMP
